#include<stdio.h>

int main(){
    int number, ucln_a,ucln_b;
    for (int i = 1;i <= ucln_a && ucln_b;i++){
    	if (a % i == 0 && b % i == 0) {
            ucln = i;
        }
	}	
    return ucln;
    int a,b;
    printf("hay nhap so nguyen duong a: \n");
    scanf("%d%d",&a);
    printf("hay nhap so nguyen duong a: \n");
    scanf("%d%d",&b);
    if (a <= 0 || b <= 0) {
        printf("nhap hai so nguyên duong!\n");
        return 1;
        int ucln = (so1, so2);
    printf("uoc chung lon nhat cua %d và %d là: %d\n", so1, so2, ucln);
	return 0;
	}

